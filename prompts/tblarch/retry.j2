上一轮骨架未通过校验，共 {{ errors|length }} 项错误：
{% for e in errors %}- {{ e }}
{% endfor %}

上一轮骨架 JSON（结构与内容已基本正确）：以它为基础做最小修改——已有格子的
结构（colspan/rowspan/style/行列）与文本保持完全不变，仅补入上述缺失的源池
文本（拼进其源碎片相邻格或独立成格）、修正上述结构问题，不要重排或删改其余部分：
{{ skeleton_json }}

原始输入重列如下：
{{ context }}

请输出修正后的完整 JSON（所有表骨架）。
